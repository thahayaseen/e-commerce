<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart - WeebsCorner</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #5F42E8;
            --secondary-color: #4A34B7;
            --accent-color: #FFD700;
        }
        
        body {
            font-family: 'Roboto', sans-serif;
        }

        .navbar {
            background-color: var(--primary-color);
            padding: 15px 0;
        }

        .navbar-brand {
            font-weight: bold;
            font-size: 28px;
            color: white !important;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }

        .nav-link {
            color: white !important;
            margin-right: 20px;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .nav-link:hover {
            color: var(--accent-color) !important;
        }

        .navbar-nav .nav-item:last-child .nav-link {
            margin-right: 0;
        }

        .navbar-icons {
            display: flex;
            align-items: center;
        }

        .navbar-icons .nav-link {
            padding: 0 10px;
        }

        .star-rating {
            color: var(--accent-color);
        }

        .hover-text-danger:hover {
            color: #dc3545 !important;
        }

        .card {
            border: none;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .btn-primary {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .btn-primary:hover {
            background-color: var(--secondary-color);
            border-color: var(--secondary-color);
        }

        .btn-outline-secondary {
            color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .btn-outline-secondary:hover {
            background-color: var(--primary-color);
            color: white;
        }

        .quantity-input {
            width: 40px;
            text-align: center;
            border: 1px solid #ced4da;
            border-radius: 0;
        }

        .empty-cart-container {
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 40px;
        }

        .continue-shopping-btn {
            transition: transform 0.3s ease;
        }

        .continue-shopping-btn:hover {
            transform: translateY(-3px);
        }

        /* Unlisted product styles */
        .unlisted-product {
            opacity: 0.6;
            background-color: #f8f9fa;
            border-left: 4px solid #dc3545;
        }

        .unlisted-badge {
            background-color: #dc3545;
            color: white;
            font-size: 0.75rem;
            padding: 2px 8px;
            border-radius: 12px;
            display: inline-block;
            margin-left: 8px;
        }

        /* Out of stock product styles */
        .outofstock-product {
            opacity: 0.6;
            background-color: #fff3cd;
            border-left: 4px solid #ffc107;
        }

        .outofstock-badge {
            background-color: #ffc107;
            color: #212529;
            font-size: 0.75rem;
            padding: 2px 8px;
            border-radius: 12px;
            display: inline-block;
            margin-left: 8px;
        }

        /* Updated disabled controls - only disable plus button */
        .disabled-plus {
            pointer-events: none;
            opacity: 0.5;
        }

        .checkout-warning {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 8px;
            padding: 12px;
            margin-bottom: 20px;
        }

        .stock-info {
            font-size: 0.85rem;
            color: #6c757d;
            margin-top: 4px;
        }

        .stock-low {
            color: #dc3545;
            font-weight: 600;
        }

        .stock-zero {
            color: #dc3545;
            font-weight: 700;
        }

        @media (max-width: 768px) {
            .navbar-brand {
                font-size: 24px;
            }
            
            .card-body {
                padding: 1rem;
            }
        }
    </style>
</head>

<body class="bg-light">
    <header class="bg-gradient text-white shadow">
        <nav class="navbar navbar-expand-md p-0 navbarbg">
            <div class="container-sm">
                <a class="navbar-brand fs-4 fw-bold" href="/">WeebsCorner</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar container-fluid" id="navbarNav">
                    <ul class="navbar-nav p-0">
                        <li class="nav-item">
                            <a class="nav-link" href="/product">All Products</a>
                        </li>
                       
                    </ul>
                    <div class="d-flex">
                        <a href="/wishlist" class="btn btn-link text-white me-2">
                            <i class="fas fa-heart"></i>
                        </a>
                        <a href="/user/mydash" class="btn btn-link text-white me-2">
                            <i class="fas fa-user"></i>
                        </a>
                      
                        <a href="/logout" class="btn btn-link text-white">
                            <i class="fas fa-sign-out-alt"></i>
                        </a>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <% if(!cart.product.length>0){ %>
    <div class="container">
        <div class="row justify-content-center align-items-center" style="height: 80vh;">
            <div class="col-md-6">
                <div class="empty-cart-container text-center">
                    <i class="fas fa-shopping-cart fa-4x mb-3 text-muted"></i>
                    <h2 class="mb-3">Your cart is empty</h2>
                    <p class="text-muted mb-4">Need inspiration? Check out our recommended items to begin building the extraordinary!</p>
                    <a href="/" class="btn btn-primary continue-shopping-btn px-4 py-2">
                        <i class="fas fa-arrow-left me-2"></i>Continue Shopping
                    </a>
                </div>
            </div>
        </div>
    </div>
    <% } else { %>
    <%
        // Check if there are any unlisted products
        let hasUnlistedProducts = false;
        let outOfstock = false;
        cart.product.forEach(product => {
            if (product.productid.unlist === true) {
                hasUnlistedProducts = true;
            }
        });
        cart.product.forEach(product => {
            if (product.productid.stock === 0 || product.productid.stock < product.quantity) {
                outOfstock = true;
            }
        });
    %>
    <div class="container py-5">
        <div class="row">
            <div class="col-lg-8">
                <h1 class="mb-4">Your Shopping Cart</h1>
                
                <% if (hasUnlistedProducts) { %>
                <div class="checkout-warning">
                    <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                    <strong>Notice:</strong> Some items in your cart are currently unavailable and have been marked for removal. Please remove these items to proceed with checkout.
                </div>
                <% } %>
                <% if (outOfstock) { %>
                <div class="checkout-warning">
                    <i class="fas fa-exclamation-triangle text-warning me-2"></i>
                    <strong>Notice:</strong> Some items in your cart are currently out of stock or exceed available quantity. Please adjust quantities or remove these items to proceed with checkout.
                </div>
                <% } %>

                <div class="card mb-3">
                    <div class="card-body">
                        <% cart.product.forEach(product => { %>
                        <%
                            let isOutOfStock = product.productid.stock === 0 || product.productid.stock < product.quantity;
                            let productClass = '';
                            if (product.productid.unlist === true) {
                                productClass = 'unlisted-product';
                            } else if (isOutOfStock) {
                                productClass = 'outofstock-product';
                            }
                        %>
                        <div class="row mb-4 align-items-center <%= productClass %>">
                            <div class="col-md-2">
                                <img src="<%= product.productid.images[0] %>" alt="<%= product.productid.name %>" class="img-fluid rounded">
                            </div>
                            <div class="col-md-4">
                                <h5 class="card-title mb-1">
                                    <%= product.productid.name %>
                                    <% if (product.productid.unlist === true) { %>
                                        <span class="unlisted-badge">Unavailable</span>
                                    <% } else if (product.productid.stock === 0) { %>
                                        <span class="outofstock-badge">Out of Stock</span>
                                    <% } else if (product.productid.stock < product.quantity) { %>
                                        <span class="outofstock-badge">Insufficient Stock</span>
                                    <% } %>
                                </h5>
                                
                                <% if (product.productid.unlist !== true) { %>
                                    <div class="stock-info">
                                        <% if (product.productid.stock === 0) { %>
                                            <span class="stock-zero">
                                                <i class="fas fa-times-circle me-1"></i>
                                                Out of Stock
                                            </span>
                                        <% } else if (product.productid.stock < product.quantity) { %>
                                            <span class="stock-low">
                                                <i class="fas fa-exclamation-triangle me-1"></i>
                                                Only <%= product.productid.stock %> left in stock (you have <%= product.quantity %> in cart)
                                            </span>
                                        <% } else if (product.productid.stock <= 5) { %>
                                            <span class="stock-low">
                                                <i class="fas fa-exclamation-circle me-1"></i>
                                                Only <%= product.productid.stock %> left in stock
                                            </span>
                                        <% } else { %>
                                            <span>
                                                <i class="fas fa-check-circle text-success me-1"></i>
                                                <%= product.productid.stock %> in stock
                                            </span>
                                        <% } %>
                                    </div>
                                <% } else { %>
                                <p class="text-danger mb-1">
                                    <i class="fas fa-times-circle me-1"></i>
                                    This item is currently unavailable
                                </p>
                                <% } %>
                                
                            </div>
                            <div class="col-md-3">
                                <div class="input-group">
                                    <!-- Minus button is always enabled for out of stock/unlisted items -->
                                    <button class="btn btn-outline-secondary minusbtn" type="button">
                                        <i class="fas fa-minus"></i>
                                    </button>
                                    <span data-quntity="<%= product.quantity %>" data-stock="<%= product.productid.stock %>" data-productid="<%= product.productid._id %>" class="quantity-input quantityid py-2 <%= (product.productid.unlist === true || isOutOfStock) ? 'bg-light text-muted' : '' %>">
                                        <%= product.quantity %>
                                    </span>
                                    <!-- Plus button is disabled for out of stock/unlisted items -->
                                    <button class="btn btn-outline-secondary plusbtn <%= (product.productid.unlist === true || isOutOfStock) ? 'disabled-plus' : '' %>" type="button" <%= (product.productid.unlist === true || isOutOfStock) ? 'disabled' : '' %>>
                                        <i class="fas fa-plus"></i>
                                    </button>
                                </div>
                            </div>
                            <div class="col-md-3 text-end">
                                <% if (product.productid.unlist !== true && !isOutOfStock) { %>
                                <p class="fw-bold mb-0">
                                    Total: ₹<span class="productprice"><%=Math.floor(product.price* product.quantity ) %></span>
                                </p>
                                <p class="mb-1 text-muted">
                                    Price: ₹<span><%= Math.floor(product.price) %></span>
                                </p>
                                <% } else { %>
                                <p class="fw-bold mb-0 text-muted text-decoration-line-through productprice">
                                    Total: ₹<span><%=Math.floor(product.price* product.quantity ) %></span>
                                </p>
                                <p class="mb-1 text-danger small">
                                    <% if (product.productid.unlist === true) { %>
                                        Item unavailable
                                    <% } else if (product.productid.stock === 0) { %>
                                        Out of stock
                                    <% } else { %>
                                        Insufficient stock
                                    <% } %>
                                </p>
                                <% } %>
                                <button class="btn btn-link text-muted p-0 hover-text-danger deletebtn" onclick="remove(event,'<%= product.productid._id %>')" data-productid="<%= product.productid._id %>">
                                    <i class="fas fa-trash"></i> Remove
                                </button>
                            </div>
                        </div>
                        <% if (cart.product.indexOf(product) !== cart.product.length - 1) { %>
                            <hr>
                        <% } %>
                        <% }) %>
                    </div>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="card mt-4 sticky-top" style="top: 20px;">
                    <div class="card-body">
                        <h5 class="card-title mb-4">Order Summary</h5>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Subtotal</span>
                            <span class="fw-bold summerytoatal">₹<%= cart.totalprice.toFixed(2) %></span>
                        </div>
                        <div class="d-flex justify-content-between mb-3">
                            <span>Estimated shipping</span>
                            <span class="text-muted">Calculated at checkout</span>
                        </div>
                        <hr>
                        <div class="d-flex justify-content-between mb-4">
                            <span class="fw-bold">Total</span>
                            <span class="fw-bold toatals">₹<%= cart.totalprice.toFixed(2) %></span>
                        </div>
                        
                        <% if (hasUnlistedProducts || outOfstock) { %>
                        <button class="btn btn-secondary w-100 py-2" disabled>
                            <i class="fas fa-exclamation-triangle me-2"></i>
                            <% if (hasUnlistedProducts && outOfstock) { %>
                                Cannot Checkout - Fix Cart Issues
                            <% } else if (hasUnlistedProducts) { %>
                                Cannot Checkout - Remove Unavailable Items
                            <% } else { %>
                                Cannot Checkout - Fix Stock Issues
                            <% } %>
                        </button>
                        <p class="text-danger small mt-2 text-center">
                            <i class="fas fa-info-circle me-1"></i>
                            <% if (hasUnlistedProducts && outOfstock) { %>
                                Remove unavailable items and fix stock issues to proceed
                            <% } else if (hasUnlistedProducts) { %>
                                Remove unavailable items to proceed
                            <% } else { %>
                                Adjust quantities or remove out of stock items to proceed
                            <% } %>
                        </p>
                        <% } else { %>
                        <a href="/checkout" class="btn btn-primary w-100 py-2">
                            <i class="fas fa-lock me-2"></i>Proceed to Checkout
                        </a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <% } %>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
   function remove(e,pid){
    

        console.log('clicked',e);
        
        // Get the product ID from the data attribute
        const productId = pid
        console.log(productId);
        
        Swal.fire({
            title: 'Are you sure?',
            text: "Do you want to remove this item from the cart?",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Yes, remove it!'
        }).then((result) => {
            if (result.isConfirmed) {
                fetch('/cart/delete', {
                    method: 'DELETE',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ 
                        // index: index,  // Use the index from forEach
                        productId: productId  // Also send product ID for additional verification
                    }),
                })
                .then(res => res.json())
                .then(res => {
                    if (res.success) {
                        window.location.href = '/cart';
                    } else {
                        // Handle delete failure
                        if (res.unlist) {
                            Swal.fire({
                                icon: 'info',
                                title: 'Product Unavailable',
                                text: res.message || 'This product is no longer available.',
                                confirmButtonText: 'Refresh Cart'
                            }).then(() => {
                                window.location.href = '/cart';
                            });
                        } else {
                            Swal.fire({
                                icon: 'error',
                                title: 'Unable to Remove',
                                text: res.message || 'An error occurred while removing the item.'
                            });
                        }
                    }
                })
                .catch(error => {
                    console.error('Error deleting item from cart:', error);
                    Swal.fire({
                        icon: 'error',
                        title: 'Connection Error',
                        text: 'An error occurred while removing the item from the cart. Please try again.'
                    });
                });
            }
        });
 
}
</script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
    <script src="/js/user/cart.js"></script>
</body>

</html>